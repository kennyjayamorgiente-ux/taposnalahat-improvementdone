# Grace Period Checker Configuration
# Copy these to your main .env file or create a separate .env file for the checker

# Database Configuration (same as your main app)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=merge1

# Grace Period Settings
GRACE_PERIOD_MINUTES=15

# Optional: Check frequency (for logging purposes)
CHECK_FREQUENCY_MINUTES=5
